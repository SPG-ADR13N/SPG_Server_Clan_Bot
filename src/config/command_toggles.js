// src/config/command_toggles.js

export const CommandToggles = {
    // Keep these active (from your birthday list)
    birthday_set: true,
    birthday_list: true,
    announcementslot: true,
    
    // --- LEVELING COMMANDS ---
    level_dashboard: true, // 🌟 ADD THIS LINE TO FIX THE DASHBOARD
    level: true,           // Add your other leveling commands here too if needed
    leveladd: true,
    levelset: true,
    
    // --- USELESS COMMANDS TO HIDE FROM EVERYONE ---
    // Change any of these to true if you ever want them back!
    weather: false,
    meme: false,
    coinflip: false,
    poll: false,
};
